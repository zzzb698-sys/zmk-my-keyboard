# 启用 zb60 所需的核心功能
CONFIG_ZMK_KEYBOARD=y
CONFIG_ZMK_KSCAN=y
CONFIG_ZMK_KEYSCAN_GPIO_MATRIX=y  # 若用 GPIO 矩阵扫描（绝大多数键盘）
CONFIG_ZMK_USB=y                  # 启用 USB 连接（按需开启）
CONFIG_ZMK_BLE=n                  # 若不用 BLE 则关闭（可选）
# 其他自定义配置（如背光、编码器等）可加在这里
